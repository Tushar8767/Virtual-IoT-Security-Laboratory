# Virtual IoT Security Laboratory — Backend
# Python dependencies
# ============================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Data Validation ---
pydantic==2.10.3
pydantic-settings==2.7.0

# --- Database ---
motor==3.7.0           # Async MongoDB driver (requires pymongo>=4.9,<4.11)
pymongo==4.10.1        # MongoDB sync driver (for scripts)

# --- MQTT ---
aiomqtt==2.3.0         # Async MQTT client (wraps paho-mqtt)
paho-mqtt==2.1.0       # MQTT client

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# --- Utilities ---
python-dotenv==1.0.1
structlog==24.4.0
httpx==0.28.1          # HTTP client for testing
anyio==4.7.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.28.1
